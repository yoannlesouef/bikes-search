/**
 * Local development config — copy to config.js and fill in your token.
 * config.js is git-ignored; never commit real credentials.
 *
 * In production, inject CATALOG_API_KEY via your hosting platform's
 * environment variable / build injection step.
 */
window.CATALOG_API_KEY = 'YOUR_AIRTABLE_PERSONAL_ACCESS_TOKEN';
