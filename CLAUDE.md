# Claude Instructions — bikes-search

## Project
A bike search application. See `specs/PRD.md` for goals and `specs/architecture.md` for system design.

## Stack
> TODO: Define stack (e.g. Next.js, FastAPI, PostgreSQL, etc.)

## Key Commands
```bash
# TODO: add install, dev, test, build commands
```

## Conventions
- Follow feature specs in `specs/features/` before implementing
- One feature per branch
- Write tests alongside implementation, not after
- Do not add speculative abstractions — implement only what the spec requires

## What to avoid
- Do not modify `specs/` files unless explicitly asked
- Do not add error handling for scenarios that can't happen
- Do not create helper utilities for one-time operations
